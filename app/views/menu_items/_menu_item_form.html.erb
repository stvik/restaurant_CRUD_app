<%= form_for @menu_item do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :price %>
	<%= f.number_field :price, value: number_with_precision(f.object.price, precision: 2),step: 0.01%>

	<%= f.submit submit_text %>
<% end %>
