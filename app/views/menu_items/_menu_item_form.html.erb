<%= form_for @menu_item do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :price %>
	<%= f.number_field :price, value: number_with_precision(f.object.price,precision: 2)%>

	<%= f.submit submit_text %>
<% end %>
