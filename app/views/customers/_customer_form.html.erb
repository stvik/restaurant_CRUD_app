

<%= form_for @customer do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :gender %>
	<%= f.select :gender, options_for_select(['Male',"Female"], selected: @customer.gender) %>

	<%= f.submit submit_text %>

<% end %>